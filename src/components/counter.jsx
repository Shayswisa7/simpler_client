import React, { useState } from "react";
const Counter = ({name}) =>{
  return (<React.Fragment>
    <button>+</button>
    <button>-</button>
    <button>בצד</button>
  </React.Fragment>);
}

export default Counter;