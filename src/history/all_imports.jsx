import { useSelector } from 'react-redux';
import { setValuesByKey, setValues, addFriend } from '../redux/user';

/*export const all_Reducer = [
  //Reducers variables.
  { name: 'item', function: useSelector((state) => state.items_in_order) },
  { name: 'user', function: useSelector((state) => state.user_reducer) },

  //Reducers methodes.
  { name: 'setValues', setValues: setValues() },
];*/
